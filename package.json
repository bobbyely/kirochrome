{
  "name": "kirochrome",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm run build -w @kirochrome/shared && npm run build -w @kirochrome/server && npm run build -w @kirochrome/web",
    "dev": "npm run build -w @kirochrome/shared && node scripts/dev.mjs",
    "start": "npm run build && npm start -w @kirochrome/server",
    "typecheck": "tsc -b packages/shared packages/server && npm run typecheck -w @kirochrome/web",
    "test": "npm run build && npm run test:unit && npm run test:server",
    "test:unit": "node --experimental-strip-types --test 'packages/web/src/__tests__/*.test.ts'",
    "test:server": "node --test --test-timeout=45000 'packages/server/test/*.test.mjs'"
  },
  "devDependencies": {
    "@types/node": "22.18.13",
    "@types/ws": "8.5.13",
    "typescript": "5.9.3"
  }
}
